'''
Created on Feb 2, 2015

@author: amagoon
'''

def title(line):
    key = '<title>'
    findit = line.find(key)
    if findit != -1:
        endit = '</title>'
        stopit = line.find(endit)
        return line[findit+7:stopit]
    else:
        return ''
